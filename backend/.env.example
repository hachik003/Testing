# Database Configuration (Railway-hosted MySQL)
# Copy this file to /.env (in the project root, not in backend folder)
# Ask the project owner for the actual database password

DB_HOST=metro.proxy.rlwy.net
DB_PORT=23692
DB_USER=root
DB_PASSWORD=your_railway_password_here_ask_owner
DB_NAME=railway

# Flask Configuration
SECRET_KEY=your_secret_key_here_change_in_production

# IMPORTANT: This file should be copied to the project ROOT directory as .env
# Example: GroupF/.env (not GroupF/backend/.env)
# The .env file is ignored by git for security reasons
